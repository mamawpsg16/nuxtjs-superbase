export default defineNuxtRouteMiddleware((to, from) => {
  const runtimeConfig = useRuntimeConfig()
    // isAuthenticated() is an example method verifying if a user is authenticated
    console.log("GLOBAL", "SHEEWSH");
  })
  